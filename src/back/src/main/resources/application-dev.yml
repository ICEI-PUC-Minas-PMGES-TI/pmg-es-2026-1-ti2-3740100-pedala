# Perfil de desenvolvimento — aponta para o mesmo Azure SQL Server
# (Flyway ativo, SQL visivel no console)
spring:
  jpa:
    show-sql: true

  flyway:
    enabled: true

logging:
  level:
    com.pedala: DEBUG
    org.hibernate.SQL: DEBUG
